pipeline:
  name: bench-distinct-by-field

nodes:
  - type: source
    name: source
    config:
      name: source
      type: csv
      path: bench://input.csv
      options:
        has_header: true
      schema:
        - { name: f0, type: string }
        - { name: f1, type: string }

  - type: transform
    name: dedup_by
    input: source
    config:
      cxl: |
        emit out = f0
        distinct by f0

  - type: output
    name: sink
    input: dedup_by
    config:
      name: sink
      type: csv
      path: bench://output.csv
      include_unmapped: true
