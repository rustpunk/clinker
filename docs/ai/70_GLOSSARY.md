# AI Onboarding: Glossary

Purpose: Define repository-specific terms that AI agents should validate before using them in explanations or code review.

## Source Evidence

Validate this page against:

- `CLAUDE.md`
- `examples/pipelines/**/*.yaml`
- `crates/*/Cargo.toml`
- `rg "CXL|pipeline|channel|composition|combine|aggregate|route|merge|source|output" docs crates examples`

Existing files under `docs/*` may be stale. Treat them as secondary context only; any glossary term found in `docs/*` must be validated against code, manifests, tests, examples, or safe local commands before being copied here.

## Product Terms

| Term | Meaning | Where it appears | Related terms | Confidence |
|---|---|---|---|---|
| Clinker | A bounded-memory, single-process, finite-batch DAG executor for ETL-style pipelines written in YAML and CXL. | `crates/clinker/src/main.rs`; `docs/user/src/getting-started/concepts.md`; `docs/ai/30_DESIGN_RULES.md` | pipeline, CXL, DAG, bounded memory | High |
| Finite batch job | The run model: sources have an end, the DAG drains, and the process exits. "Streaming" does not mean an unbounded service. | `docs/user/src/getting-started/concepts.md`; `crates/clinker-exec/src/source/mod.rs` | RecordSource, streaming, source | High |
| Single-process execution | One `clinker run` invocation runs in one OS process; parallelism is in-process through threads, channels, and Rayon. | `docs/user/src/getting-started/concepts.md`; `docs/ai/30_DESIGN_RULES.md`; `crates/clinker-exec/src/executor/mod.rs` | concurrency, PipelineExecutor, bounded channels | High |
| DAG | Directed acyclic graph of pipeline nodes. Pipeline wiring is validated as acyclic before execution. | `docs/user/src/getting-started/concepts.md`; `crates/clinker-plan/src/plan/execution/mod.rs`; `crates/clinker-core-types/src/graph.rs` | ExecutionPlanDag, PlanNode, NameGraph | High |
| Pipeline | A YAML-defined Clinker job with `pipeline:` metadata, a flat `nodes:` graph, and optional `error_handling:`. | `crates/clinker-plan/src/config/pipeline.rs`; `docs/user/src/pipelines/structure.md`; `examples/pipelines/*.yaml` | PipelineConfig, PipelineMeta, nodes | High |
| Node | A named pipeline graph vertex selected by a `type:` discriminator. Current code variants are `source`, `transform`, `aggregate`, `route`, `merge`, `combine`, `output`, `reshape`, `cull`, `envelope`, and `composition`. | `crates/clinker-plan/src/config/pipeline_node.rs`; `docs/user/src/nodes/*.md`; `examples/pipelines/*.yaml` | PipelineNode, PlanNode, node ports | High |
| Node port | A named output branch or input endpoint referenced as `node.port`; used by route branches, cull side outputs, and composition/envelope wiring. | `docs/user/src/getting-started/concepts.md`; `docs/user/src/nodes/route.md`; `docs/user/src/nodes/cull.md`; `crates/clinker-plan/src/plan/execution/mod.rs` | route, removed_to, body, header | High |
| Channel | A per-tenant `channel/<id>/` folder (manifest + per-target overlays) that overlays a pipeline's declared config/variable defaults and structure for a tenant, environment, or business unit. | `crates/clinker-channel/src/lib.rs`; `docs/user/src/pipelines/channels.md`; `examples/multitenant/channel/` | OverlayResolution, per-target overlay, group derivation, scoped vars | High |
| Composition | A reusable pipeline fragment declared in `.comp.yaml` and invoked by a `composition` node with config, port, and resource bindings. | `docs/user/src/pipelines/compositions.md`; `examples/pipelines/compositions/*.comp.yaml`; `crates/clinker-plan/src/config/composition/` | CompositionBodyId, BoundBody, resources | High |

## Primary Code Evidence

| Term | Meaning | Where it appears | Related terms | Confidence |
|---|---|---|---|---|
| `clinker` crate | Main user-facing CLI binary. It runs pipelines, explains plans/diagnostics, collects metrics, and wires channels/storage/runtime params. | `crates/clinker/Cargo.toml`; `crates/clinker/src/main.rs`; `crates/clinker/tests/` | PipelineExecutor, run, metrics, explain | High |
| `clinker-record` crate | Shared record/value/schema model: `Value`, `Record`, `Schema`, storage traits, group keys, provenance, document context, and accumulators. | `crates/clinker-record/src/lib.rs`; `crates/clinker-record/Cargo.toml` | Record, Value, Schema, FieldStr | High |
| `clinker-core-types` crate | Leaf vocabulary crate for spans, diagnostics, name graphs, and DLQ categories. | `crates/clinker-core-types/src/lib.rs`; `docs/ai/20_CRATE_MAP.md` | Span, Diagnostic, NameGraph, DlqErrorCategory | High |
| `cxl` crate | CXL parser, resolver, type checker, analyzer, planner helpers, and evaluator. | `crates/cxl/src/lib.rs`; `crates/cxl/Cargo.toml`; `docs/engine/src/cxl-internals.md` | CXL, Parser, TypedProgram, ProgramEvaluator | High |
| `cxl-cli` crate | Standalone CXL command-line tool whose Clap command name is `cxl`; checks, evaluates, and formats CXL. | `crates/cxl-cli/src/main.rs`; `crates/cxl-cli/Cargo.toml` | CXL, check, eval, fmt | High |
| `clinker-format` crate | Streaming readers/writers and format utilities for CSV, JSON, XML, fixed-width, HL7, X12, EDIFACT, SWIFT, envelopes, document indexes, splitting, and reopenable sources. | `crates/clinker-format/src/lib.rs`; `crates/clinker-format/src/traits.rs`; `docs/user/src/formats/*.md` | FormatReader, FormatWriter, EnvelopeFramer | High |
| `clinker-plan` crate | YAML/config parsing, validation, schema binding, CXL type checking against row types, composition binding, and execution DAG construction. | `crates/clinker-plan/src/lib.rs`; `crates/clinker-plan/src/config/`; `crates/clinker-plan/src/plan/` | PipelineConfig, CompiledPlan, ExecutionPlanDag | High |
| `clinker-exec` crate | Runtime executor and operators: ingest, dispatch, transforms, aggregate, combine, route, merge, reshape, cull, output, DLQ, metrics, memory, spill, and record sources. | `crates/clinker-exec/src/lib.rs`; `crates/clinker-exec/src/executor/mod.rs`; `crates/clinker-exec/tests/` | PipelineExecutor, RecordSource, MemoryArbitrator | High |
| `clinker-channel` crate | Channel/group folder-overlay discovery, group derivation, overlay resolution, and source staging-copy support. | `crates/clinker-channel/src/lib.rs`; `crates/clinker-channel/tests/`; `docs/user/src/pipelines/channels.md` | OverlayResolution, SourceStager, DottedPath | High |
| `clinker-net` crate | Finite-pull network transport, currently REST, adapted into executor `RecordSource` inputs. | `crates/clinker-net/src/lib.rs`; `crates/clinker-net/tests/` | REST source, RecordSource, SourceInput::Records | High |
| `clinker-schema` crate | `.schema.yaml` parsing, workspace schema discovery, schema indexes, and pipeline schema-reference validation. | `crates/clinker-schema/src/lib.rs`; `examples/pipelines/retract-demo/*.schema.yaml` | SourceSchema, SchemaIndex, SchemaWarning | High |
| `clinker-bench-support` crate | Shared benchmark/test utilities: generated data, cached data, IO helpers, combine generators, and optional allocation accounting. | `crates/clinker-bench-support/src/lib.rs`; `crates/clinker-bench-support/tests/` | DataSpec, RecordFactory, AccountingAlloc | High |
| `clinker-benchmarks` crate | End-to-end benchmark harness over benchmark YAML configs and generated input data. | `crates/clinker-benchmarks/src/lib.rs`; `benches/pipelines/**/*.yaml` | BenchPipelineRunner, BenchReport | High |
| `reserve/` | Separate non-workspace Cargo package named `clinker`, apparently used as a crates.io name-reservation placeholder rather than runtime code. | `reserve/Cargo.toml`; `reserve/README.md`; `docs/ai/20_CRATE_MAP.md` | workspace crates, crate reservation | Medium |
| `PipelineConfig` | Top-level deserialized YAML pipeline structure with `pipeline`, `nodes`, `error_handling`, notes, and source hash. | `crates/clinker-plan/src/config/pipeline.rs` | PipelineMeta, PipelineNode, ErrorHandlingConfig | High |
| `PipelineMeta` | Pipeline-level metadata and global settings such as name, memory, batch size, vars, date formats, rules path, concurrency, provenance, and metrics. | `crates/clinker-plan/src/config/pipeline.rs`; `docs/user/src/pipelines/structure.md` | vars, MemoryConfig, MetricsConfig | High |
| `PipelineNode` | Source-level unified enum for every YAML node variant; preserves YAML spans via custom serde-saphyr dispatch. | `crates/clinker-plan/src/config/pipeline_node.rs` | PlanNode, NodeHeader, Spanned | High |
| `PlanNode` | Planned execution-node enum consumed by planning/explain/execution DAG logic. | `crates/clinker-plan/src/plan/execution/mod.rs`; `docs/engine/src/execution-model.md` | ExecutionPlanDag, PlanEdge, DependencyType | High |
| `CompiledPlan` | Validated, schema-bound, typechecked plan artifact passed to the executor. | `crates/clinker-plan/src/plan/compiled.rs`; `crates/clinker-exec/src/executor/mod.rs`; `docs/ai/30_DESIGN_RULES.md` | CompileArtifacts, PipelineExecutor | High |
| `ExecutionPlanDag` | Petgraph-backed execution graph of planned nodes and edges. | `crates/clinker-plan/src/plan/execution/mod.rs`; `docs/engine/src/architecture.md` | DAG, PlanNode, PlanEdge | High |
| `PipelineExecutor` | Runtime entry point that executes a compiled plan with source readers/writers and run parameters. | `crates/clinker-exec/src/executor/mod.rs`; `crates/clinker/src/main.rs` | PipelineRunParams, ExecutionReport, SourceReaders | High |
| `PipelineRunParams` | Runtime settings threaded into executor runs, separate from compiled plan config. | `crates/clinker-exec/src/executor/params.rs`; `crates/clinker-exec/src/executor/mod.rs` | ExecutionReport, storage config, metrics | High |
| `ExecutionReport` | Run result summary returned by execution, including counts/metrics surfaces such as per-source rollback cursors. | `crates/clinker-exec/src/executor/params.rs`; `docs/engine/src/correlation-lifecycle.md` | PipelineCounters, RetractionMetrics | High |
| `Record` | In-memory row payload plus schema/provenance/document context used throughout execution. | `crates/clinker-record/src/record/mod.rs`; `crates/clinker-record/src/lib.rs` | Value, Schema, RecordPayload | High |
| `Value` | Clinker's typed scalar/compound value enum used for records, CXL evaluation, grouping, spill, and output. | `crates/clinker-record/src/value.rs`; `docs/ai/60_PERFORMANCE_NOTES.md` | NULL, FieldStr, Type | High |
| `FieldStr` | Optimized string storage used by `Value::String`; supports inline short strings and shared/unique long strings. | `crates/clinker-record/src/field_str.rs`; `docs/ai/60_PERFORMANCE_NOTES.md` | long_unique, Value::String | High |
| `Schema` | Ordered field/schema metadata for records and type checking. | `crates/clinker-record/src/schema.rs`; `crates/clinker-record/src/lib.rs` | SchemaBuilder, FieldMetadata | High |
| `SchemaBuilder` | Preferred local builder for creating `Arc<Schema>` values while keeping fields and metadata aligned. | `crates/clinker-record/src/schema.rs`; `docs/ai/40_COMMON_PATTERNS.md` | Schema, FieldMetadata | High |
| `RecordStorage` | Trait abstraction used by CXL/evaluator paths to access record-like storage. | `crates/clinker-record/src/storage.rs`; `docs/ai/40_COMMON_PATTERNS.md` | Record, RecordView, ProgramEvaluator | High |
| `FieldResolver` | Trait for resolving fields by name during expression evaluation. | `crates/clinker-record/src/resolver.rs`; `docs/ai/40_COMMON_PATTERNS.md` | HashMapResolver, WindowContext | High |
| `FormatReader` | Send-only streaming trait for decoding byte-oriented sources into records, with optional document/envelope hooks. | `crates/clinker-format/src/traits.rs`; `docs/ai/40_COMMON_PATTERNS.md` | RecordSource, FormatWriter, EnvelopeEvent | High |
| `FormatWriter` | Send-only streaming trait for writing records, flushing output, and optionally framing document boundaries. | `crates/clinker-format/src/traits.rs`; `crates/clinker-format/src/splitting/mod.rs` | Output, SplittingWriter, bytes_written | High |
| `RecordSource` | Transport-agnostic finite record-yielding trait; files adapt through `FormatReader`, while network/database-like sources can implement it directly. | `crates/clinker-exec/src/source/mod.rs`; `crates/clinker-net/src/lib.rs` | SourceInput, FormatReader, finite inputs | High |
| `SourceInput` | Runtime enum for source ingest inputs: resolved file slots or a boxed `RecordSource`. | `crates/clinker-exec/src/source/mod.rs`; `crates/clinker-exec/src/executor/mod.rs` | SourceReaders, MultiFileFormatReader | High |
| `ValidatedPath` | Proof-token wrapper around paths that passed Clinker's security validation. | `crates/clinker-plan/src/security.rs`; `docs/ai/30_DESIGN_RULES.md` | path security, proof tokens | High |
| `Spanned<T>` | serde-saphyr wrapper carrying YAML source locations for diagnostics. | `crates/clinker-plan/src/yaml.rs`; `crates/clinker-plan/src/config/pipeline_node.rs` | CxlSource, Span, miette | High |
| `CxlSource` | Wrapper for CXL source text parsed from YAML while preserving source-location context. | `crates/clinker-plan/src/yaml.rs`; `docs/ai/30_DESIGN_RULES.md` | CXL, Spanned, diagnostics | High |
| `Diagnostic` | Shared structured diagnostic type exported by `clinker-core-types`. | `crates/clinker-core-types/src/diagnostic.rs`; `crates/clinker-core-types/src/lib.rs` | Severity, LabeledSpan, DiagnosticPayload | High |
| `NameGraph` | Name-keyed graph helper in the core-types crate. | `crates/clinker-core-types/src/graph.rs`; `crates/clinker-core-types/src/lib.rs` | DAG, PlanEdge | High |

## Pipeline Terms

| Term | Meaning | Where it appears | Related terms | Confidence |
|---|---|---|---|---|
| Source node | Entry-point node that reads finite records from a file or other finite transport. It has no upstream input. | `docs/user/src/nodes/source.md`; `crates/clinker-plan/src/config/source.rs`; `PipelineNode::Source` | SourceConfig, RecordSource, InputFormat | High |
| Transform node | Per-record CXL projection/filter/enrichment node. It can emit fields, filter rows, validate, log, and fan out via `emit each`. | `docs/user/src/nodes/transform.md`; `crates/clinker-exec/src/executor/transform_dispatch.rs`; `PipelineNode::Transform` | CXL, TransformBody, ProgramEvaluator | High |
| Aggregate node | Grouping/reduction node that emits one summary row per group or window using CXL aggregate functions. | `docs/user/src/nodes/aggregate.md`; `crates/clinker-plan/src/config/aggregate.rs`; `crates/clinker-exec/src/aggregation/` | group_by, AggregateStrategyHint, accumulators | High |
| Route node | Per-record fan-out node that evaluates ordered CXL conditions and emits records to named output ports. | `docs/user/src/nodes/route.md`; `crates/clinker-plan/src/config/pipeline_node.rs`; `crates/clinker-exec/src/executor/route.rs` | RouteBody, RouteMode, ports | High |
| Merge node | Fan-in node that concatenates or interleaves upstream streams sharing a compatible schema. | `docs/user/src/nodes/merge.md`; `crates/clinker-plan/src/config/format.rs`; `crates/clinker-exec/src/executor/merge_dispatch.rs` | MergeMode, inputs, streaming interleave | High |
| Combine node | N-ary record-combining/join node using qualified inputs, a CXL `where:` predicate, match/on-miss policy, and output CXL. | `docs/user/src/nodes/combine.md`; `crates/clinker-plan/src/plan/combine.rs`; `crates/clinker-exec/src/pipeline/combine.rs` | driver, build side, IEJoin, grace hash | High |
| Output node | Sink node that writes records to a configured path and format, with projection, splitting, correlation-key visibility, and envelope reconstruction options. | `docs/user/src/nodes/output.md`; `crates/clinker-plan/src/config/output.rs`; `crates/clinker-exec/src/executor/output_dispatch.rs` | OutputConfig, FormatWriter, split | High |
| Reshape node | Blocking group operator that mutates trigger rows and/or synthesizes new rows within each `partition_by` group. | `docs/user/src/nodes/reshape.md`; `crates/clinker-plan/src/config/pipeline_node.rs`; `crates/clinker-exec/src/executor/reshape_dispatch.rs` | partition_by, `$meta.*`, synthesize | High |
| Cull node | Blocking group operator that routes whole groups matching a group-level predicate to a side-output port instead of the main output. | `docs/user/src/nodes/cull.md`; `crates/clinker-plan/src/config/pipeline_node.rs`; `crates/clinker-exec/src/executor/cull_dispatch.rs` | removed_to, partition_by, route ports | High |
| Envelope node | Node that frames a body stream into per-document documents with `preserve` or `concat` strategy and optional header/footer synthesis. | `docs/user/src/nodes/envelope.md`; `crates/clinker-plan/src/config/pipeline_node.rs`; `crates/clinker-exec/src/executor/envelope_dispatch.rs` | DocumentContext, document grain, `$doc.*` | High |
| Composition node | Call-site node for a `.comp.yaml` reusable body. It supports `use`, optional alias, input/output port bindings, config, and resources. | `docs/user/src/pipelines/compositions.md`; `crates/clinker-plan/src/config/pipeline_node.rs`; `crates/clinker-plan/src/config/composition/` | BoundBody, CompositionBodyId, resources | High |
| `input:` | Node wiring field for a single upstream reference; combine overloads it as a qualifier-to-upstream map. | `docs/user/src/pipelines/structure.md`; `docs/user/src/nodes/combine.md`; `crates/clinker-plan/src/config/node_header.rs` | inputs, qualifier, port syntax | High |
| `inputs:` | Node wiring field for multiple upstreams on merge and composition port binding contexts. | `docs/user/src/pipelines/structure.md`; `docs/user/src/pipelines/compositions.md`; `crates/clinker-plan/src/config/node_header.rs` | input, merge, composition | High |
| `body:` | Envelope node's required body input port. | `docs/user/src/nodes/envelope.md`; `crates/clinker-plan/src/config/node_header.rs`; `crates/clinker-plan/src/config/pipeline_node.rs` | envelope, header, trailer | High |
| `header:` | Envelope node input port for one header record per document grain; also a nested config map for declarative header synthesis, depending on location. | `docs/user/src/nodes/envelope.md`; `crates/clinker-plan/src/config/pipeline_node.rs`; `crates/clinker-plan/src/config/node_header.rs` | body, footer, DocumentGrain | High |
| `trailer:` | Envelope node input port accepted in config shape but currently rejected when wired, per user docs. | `docs/user/src/nodes/envelope.md`; `crates/clinker-plan/src/config/pipeline_node.rs` | envelope, header, footer | High |
| `group_by` | Aggregate grouping fields; empty means one global group. Also controls strict-vs-retraction behavior with correlation keys. | `docs/user/src/nodes/aggregate.md`; `docs/user/src/pipelines/correlation-keys.md`; `crates/clinker-plan/src/config/aggregate.rs` | aggregate, correlation key, retraction | High |
| `partition_by` | Grouping fields used by Reshape and Cull for whole-group observation. | `docs/user/src/nodes/reshape.md`; `docs/user/src/nodes/cull.md`; `crates/clinker-plan/src/config/pipeline_node.rs` | group_by, correlation group | High |
| `sort_order` | Declaration of pre-sorted source/output/order fields used by planning, streaming aggregation eligibility, and output sort behavior. | `docs/user/src/nodes/source.md`; `crates/clinker-plan/src/config/sort.rs`; `crates/clinker-plan/src/plan/properties.rs` | Ordering, SortFieldSpec, streaming aggregate | High |
| `watermark` | Source event-time declaration that stamps `$source.event_time` and lets time-windowed aggregates close windows. | `docs/user/src/nodes/source.md`; `docs/user/src/nodes/aggregate.md`; `crates/clinker-plan/src/config/source.rs`; `crates/clinker-exec/src/executor/watermark.rs` | event time, time_window, allowed_lateness | High |
| `time_window` | Aggregate config that assigns records to event-time windows such as tumbling or hopping windows. | `docs/user/src/nodes/aggregate.md`; `crates/clinker-plan/src/config/pipeline_node.rs`; `crates/clinker-exec/src/executor/time_window.rs` | watermark, tumbling, hopping | High |
| `allowed_lateness` | Operator-side tolerance for late records in time-windowed aggregates. | `crates/clinker-plan/src/config/aggregate.rs`; `crates/clinker-plan/src/config/pipeline_node.rs` | watermark, LateRecord, time_window | High |
| `correlation_key` | Per-source field or field tuple that identifies an atomic group for DLQ and retraction semantics. Captured at ingest. | `docs/user/src/pipelines/correlation-keys.md`; `crates/clinker-plan/src/config/aggregate.rs`; `docs/engine/src/correlation-lifecycle.md` | `$ck.*`, DLQ, retraction | High |
| Correlation group | Records sharing a source-local correlation-key tuple. Group identity is engine-stamped and may survive field rewrites. | `docs/user/src/pipelines/correlation-keys.md`; `docs/engine/src/correlation-lifecycle.md` | correlation_key, collateral, trigger | High |
| `propagate_ck` | Combine config selecting which correlation-key columns ride onto combine output rows: driver, all, or a named subset. | `docs/user/src/pipelines/correlation-keys.md`; `docs/user/src/nodes/combine.md`; `crates/clinker-plan/src/plan/bind_schema.rs` | correlation key, driver, build side | High |
| `include_correlation_keys` | Output flag that exposes normally hidden `$ck.<field>` columns for debugging. | `docs/user/src/pipelines/correlation-keys.md`; `crates/clinker-plan/src/config/output.rs` | `$ck.*`, OutputConfig | High |
| `on_unmapped` | Source policy for undeclared input fields: auto-widen, drop, or reject. | `docs/user/src/nodes/source.md`; `docs/user/src/formats/auto-widen.md`; `crates/clinker-plan/src/config/source.rs` | `$widened`, auto-widen, include_unmapped | High |
| Auto-widen | Schema-drift behavior that carries undeclared input fields through a hidden sidecar and expands them at output by default. | `docs/user/src/formats/auto-widen.md`; `docs/engine/src/auto-widen-internals.md`; `crates/clinker-exec/tests/auto_widen_integration.rs` | `$widened`, on_unmapped, include_unmapped | High |
| `include_unmapped` | Output flag controlling whether auto-widened/undeclared fields are surfaced at the sink. | `crates/clinker-plan/src/config/output.rs`; `docs/user/src/formats/auto-widen.md` | auto-widen, `$widened`, OutputConfig | High |
| DLQ | Dead-letter queue for records/documents/groups rejected under non-fail-fast error strategies. Entries include category, message, stage, and record context. | `docs/user/src/pipelines/error-handling.md`; `crates/clinker-exec/src/dlq.rs`; `crates/clinker-core-types/src/dlq.rs` | DlqEntry, DlqErrorCategory, error_handling | High |
| `error_handling.strategy` | Pipeline-level failure policy: fail fast, continue with DLQ, or best effort. | `docs/user/src/getting-started/concepts.md`; `crates/clinker-plan/src/config/pipeline.rs`; `crates/clinker-exec/tests/dlq_rate_threshold.rs` | DLQ, ErrorStrategy, DlqConfig | High |
| `dlq_granularity` | Per-source choice of record-level or document-level dead-lettering under continue/best-effort strategies. | `crates/clinker-plan/src/config/source.rs`; `docs/user/src/pipelines/envelope-and-doc-context.md`; `crates/clinker-exec/src/executor/document_dlq.rs` | document DLQ, envelope, StructuralReject | High |
| Trigger DLQ entry | The DLQ entry caused by the actual failure inside a correlated/document group. | `docs/user/src/pipelines/correlation-keys.md`; `docs/engine/src/correlation-lifecycle.md` | collateral, correlated, DLQ | High |
| Collateral DLQ entry | DLQ entry for a peer record rejected because another member of its group/document failed. | `docs/user/src/pipelines/correlation-keys.md`; `crates/clinker-plan/src/config/source.rs`; `docs/engine/src/correlation-lifecycle.md` | trigger, correlated, DLQ | High |
| `max_group_buffer` | Error-handling cap on per-correlation-group buffering before group-size overflow DLQ behavior. | `docs/user/src/pipelines/correlation-keys.md`; `docs/engine/src/correlation-lifecycle.md`; `crates/clinker-exec/tests/correlation_overflow_rewind.rs` | correlation group, group_size_exceeded | High |
| `long_unique` | Source-schema hint for long high-cardinality string columns; affects memory storage strategy without changing value semantics. | `docs/user/src/nodes/source.md`; `crates/clinker-record/src/field_str.rs`; `crates/clinker-exec/tests/long_unique_column.rs` | FieldStr, Value::String | High |
| File matcher | Source file discovery selector: exactly one of `path`, `glob`, `regex`, or `paths`. | `crates/clinker-plan/src/config/source.rs`; `docs/user/src/nodes/source.md`; `crates/clinker-plan/src/config/discovery.rs` | SourceConfig, DiscoveredFile, file transport | High |
| `split` output | Output-file rotation config based on record count, byte size, and optional group-key boundaries. | `crates/clinker-plan/src/config/output.rs`; `crates/clinker-format/src/splitting/mod.rs`; `docs/user/src/cookbook/file-splitting.md` | SplitConfig, SplittingWriter | High |
| `if_exists` | Output collision policy: overwrite, error, or unique suffix. | `crates/clinker-plan/src/config/output.rs`; `crates/clinker/tests/atomic_output_test.rs` | OutputConfig, unique_suffix_width | High |

## CXL Terms

| Term | Meaning | Where it appears | Related terms | Confidence |
|---|---|---|---|---|
| CXL | Clinker's per-record ETL expression language. It is parsed, resolved, typechecked, and evaluated by the `cxl` crate; it is not SQL. The repo does not clearly define the acronym expansion. | `crates/cxl/src/lib.rs`; `docs/user/src/getting-started/concepts.md`; `docs/engine/src/cxl-internals.md` | AST, TypedProgram, ProgramEvaluator | High for behavior, Low for acronym expansion |
| CXL program | A sequence of CXL statements such as `emit`, `let`, `filter`, `distinct`, and `emit each`. | `crates/cxl/src/ast.rs`; `docs/user/src/cxl/overview.md`; `docs/user/src/nodes/transform.md` | Program, Statement, Parser | High |
| `emit` | CXL statement that writes a field to the output record. Only emitted fields appear downstream unless pass-through/unmapped rules apply. | `docs/user/src/getting-started/concepts.md`; `crates/cxl/src/ast.rs`; `crates/clinker-exec/src/executor/transform_dispatch.rs` | Transform, CompiledEmit, projection | High |
| `let` | CXL statement that binds a local variable for later expressions but does not emit a field. | `docs/user/src/getting-started/concepts.md`; `crates/cxl/src/ast.rs` | emit, filter, scope | High |
| `filter` | CXL statement that drops a record when its condition evaluates false. | `docs/user/src/getting-started/concepts.md`; `crates/cxl/src/ast.rs`; `crates/cxl/src/eval/mod.rs` | SkipReason, Transform, Route | High |
| `distinct` | CXL deduplication statement, either across all output fields or by a named field. | `docs/user/src/getting-started/concepts.md`; `crates/cxl/src/ast.rs`; `crates/clinker-plan/src/plan/execution/composition.rs` | Transform, dedupe | High |
| `emit each` | CXL fan-out construct that can produce multiple output records from one input record, bounded by `max_expansion`. | `docs/user/src/cxl/emit-each.md`; `docs/user/src/nodes/transform.md`; `crates/cxl/src/ast.rs` | max_expansion, DLQ | High |
| `max_expansion` | Transform safety cap on output rows produced by `emit each` for one input record. | `docs/user/src/nodes/transform.md`; `crates/clinker-plan/src/config/pipeline_node.rs`; `crates/clinker-exec/src/executor/transform_dispatch.rs` | emit each, expansion_limit_exceeded | High |
| CXL module | CXL source unit imported/resolved through module support and `rules_path`. | `docs/user/src/cxl/modules.md`; `crates/cxl/src/module_eval.rs`; `crates/clinker-exec/src/modules.rs` | rules_path, ModuleConst, ModuleRegistry | High |
| `TypedProgram` | Typechecked CXL program returned by the typecheck pass and stored in compile artifacts. | `crates/cxl/src/typecheck/pass.rs`; `crates/clinker-plan/src/plan/bind_schema.rs`; `docs/engine/src/cxl-internals.md` | type_check, CompileArtifacts, ProgramEvaluator | High |
| `ProgramEvaluator` | Runtime evaluator that compiles and caches typed CXL programs for per-record execution. | `crates/cxl/src/eval/mod.rs`; `docs/ai/60_PERFORMANCE_NOTES.md`; `crates/clinker-exec/src/executor/transform.rs` | TypedProgram, EvalContext, compiled path | High |
| `CompiledScalar` | Compiled scalar expression used by hot paths such as aggregate filters and operator predicates. | `crates/cxl/src/eval/compiled.rs`; `crates/cxl/src/eval/mod.rs`; `docs/ai/60_PERFORMANCE_NOTES.md` | compile_scalar, EvalContext | High |
| `EvalContext` | Runtime CXL evaluation context carrying record access, stable clocks, scoped variables, window context, and system namespaces. | `crates/cxl/src/eval/context.rs`; `crates/clinker-exec/src/executor/dispatch.rs` | StableEvalContext, Clock, scoped vars | High |
| `BuiltinRegistry` | Registry of CXL builtin definitions. | `crates/cxl/src/builtins.rs`; `docs/ai/40_COMMON_PATTERNS.md` | BuiltinDef, Category, TypeTag | High |
| `QualifiedField` | Typechecker row field reference with optional qualifier, used heavily by combine and row typing. | `crates/cxl/src/typecheck/row.rs`; `crates/clinker-plan/src/plan/row_type.rs`; `crates/clinker-plan/src/plan/combine.rs` | Row, ColumnLookup, combine qualifier | High |
| `Row` | Typechecker representation of available columns for CXL name/type resolution. | `crates/cxl/src/typecheck/row.rs`; `crates/clinker-plan/src/plan/row_type.rs`; `crates/clinker-plan/src/plan/bind_schema.rs` | QualifiedField, RowTail, Type | High |
| `RowTail` | Typechecker row-tail model used for open row shapes and composition/pass-through typing. | `crates/cxl/src/typecheck/row.rs`; `crates/clinker-plan/src/plan/row_type.rs`; `crates/clinker-exec/tests/composition_binding_test.rs` | TailVarId, composition | Medium |
| `AggregateBinding` | Planned CXL aggregate binding extracted from aggregate CXL. | `crates/cxl/src/plan/mod.rs`; `crates/cxl/src/plan/extract_aggregates.rs`; `crates/clinker-exec/src/aggregation/` | CompiledAggregate, BindingArg | High |
| CXL system namespaces | Dollar-prefixed namespaces exposed to CXL, including `$vars`, `$pipeline`, `$source`, `$record`, `$window`, `$doc`, `$ck`, `$meta`, and `$widened` in specific contexts. | `docs/user/src/cxl/system-variables.md`; `docs/user/src/pipelines/channels.md`; `docs/engine/src/correlation-lifecycle.md`; `docs/engine/src/auto-widen-internals.md`; `docs/user/src/nodes/reshape.md` | scoped vars, document context, shadow columns | High |
| `$vars.*` | Static pipeline variable namespace frozen at pipeline start and overridable by channels. | `docs/user/src/pipelines/channels.md`; `crates/clinker-plan/src/config/pipeline.rs`; `crates/cxl/src/eval/context.rs` | scoped vars, channel vars | High |
| `$pipeline.*` | Pipeline-scope variable namespace with reserved engine fields and optional mutable declarations. | `docs/user/src/pipelines/channels.md`; `crates/clinker-plan/src/plan/bind_schema.rs`; `crates/cxl/src/eval/context.rs` | scoped vars, declares | High |
| `$source.*` | Source-scope variable namespace, including reserved source metadata such as file/path/count fields and per-source declarations. | `docs/user/src/pipelines/channels.md`; `docs/user/src/nodes/source.md`; `crates/clinker-exec/src/executor/ingest.rs` | source, scoped vars, watermark | High |
| `$record.*` | Per-record mutable scoped-variable namespace declared by transforms. | `docs/user/src/pipelines/channels.md`; `crates/clinker-plan/src/plan/bind_schema.rs`; `crates/cxl/src/resolve/scoped_vars.rs` | scoped vars, declares | High |
| `$window.*` | Window namespace for analytic/window functions available to transform/window evaluation. | `docs/user/src/nodes/transform.md`; `docs/user/src/cxl/windows.md`; `crates/clinker-exec/src/executor/window_runtime.rs` | analytic_window, WindowContext | High |
| `$doc.*` | Document envelope context namespace exposing declared envelope sections and fields to CXL. | `docs/user/src/pipelines/envelope-and-doc-context.md`; `crates/clinker-format/src/traits.rs`; `crates/clinker-record/src/document_context.rs` | DocumentContext, envelope, document index | High |
| `$ck.*` | Engine-managed correlation-key shadow columns. Hidden from default output and not author-written in CXL/YAML. | `docs/engine/src/correlation-lifecycle.md`; `docs/user/src/pipelines/correlation-keys.md`; `crates/clinker-plan/src/plan/bind_schema.rs` | correlation_key, include_correlation_keys | High |
| `$ck.aggregate.<name>` | Synthetic lineage column emitted by relaxed/retraction-mode aggregates so post-aggregate failures can map back to contributing source rows. | `docs/engine/src/retraction-protocol.md`; `crates/clinker-exec/tests/post_aggregate_recompute_determinism.rs` | retraction protocol, relaxed CK | High |
| `$meta.*` | Engine-written reshape audit namespace with synthetic/mutated/synthesized markers. Hidden from default writer output but available downstream. | `docs/user/src/nodes/reshape.md`; `docs/user/src/cookbook/scd-type2.md`; `crates/clinker-exec/src/executor/reshape_dispatch.rs` | Reshape, audit stamps | High |
| `$widened` | Engine-managed auto-widen sidecar for undeclared input fields. CXL cannot read its contents directly. | `docs/engine/src/auto-widen-internals.md`; `docs/user/src/formats/auto-widen.md`; `crates/clinker-exec/tests/auto_widen_integration.rs` | auto-widen, include_unmapped | High |
| Analytic window | Transform-side cross-source lookup/index feature exposed through `analytic_window` and `$window.*`. | `docs/user/src/nodes/transform.md`; `crates/clinker-plan/src/plan/index.rs`; `crates/clinker-exec/src/executor/window_runtime.rs` | WindowRuntimeRegistry, Arena, SecondaryIndex | High |
| `declares` | Transform/composition declaration surface for scoped variables available through `$pipeline`, `$source`, or `$record`. | `docs/user/src/pipelines/channels.md`; `crates/clinker-plan/src/plan/bind_schema.rs`; `crates/clinker-exec/tests/state_node_diagnostics.rs` | scoped vars, channel overrides | High |

## Execution Terms

| Term | Meaning | Where it appears | Related terms | Confidence |
|---|---|---|---|---|
| Streaming | In Clinker, row-by-row or bounded-batch execution within a finite batch job; not unbounded stream processing. | `docs/user/src/getting-started/concepts.md`; `docs/engine/src/execution-model.md`; `crates/clinker-plan/src/plan/streaming_eligibility.rs` | blocking, batch_size, StreamClass | High |
| Blocking stage | Stage that must accumulate state/input before producing output, such as hash aggregate, sort, combine build side, IEJoin, sort-merge, and correlation commit. | `docs/engine/src/execution-model.md`; `crates/clinker-exec/src/executor/dispatch.rs` | materialized buffer, spill, memory | High |
| Fused streaming path | Optimized Source -> Transform -> Output style path that avoids full materialization and holds bounded in-flight batches. | `docs/engine/src/execution-model.md`; `crates/clinker-exec/src/executor/streaming.rs`; `crates/clinker-exec/tests/transform_stream_fusion.rs` | streaming, Output, batch_size | High |
| `batch_size` | Number of events per streaming handoff batch; pipeline-level default can be overridden per transform. | `docs/user/src/nodes/transform.md`; `docs/engine/src/execution-model.md`; `crates/clinker-plan/src/config/pipeline.rs` | EventBatch, StreamEvent | High |
| `StreamEvent` | Inline ordered stream item carrying records and control events such as document-boundary punctuations. | `crates/clinker-exec/src/executor/stream_event.rs`; `docs/ai/40_COMMON_PATTERNS.md` | Punctuation, SourceStream, EventBatch | High |
| `Punctuation` | Ordered stream-control event, including document open/close boundaries, carried inline with records. | `crates/clinker-exec/src/executor/stream_event.rs`; `docs/engine/src/execution-model.md` | DocumentOpen, DocumentClose, StreamEvent | High |
| `StructuralReject` | Stream event payload for structural document/source rejection. | `crates/clinker-exec/src/executor/stream_event.rs`; `crates/clinker-exec/src/executor/document_dlq.rs` | document DLQ, dlq_granularity | High |
| Memory arbitrator | Run-scoped subsystem that tracks memory consumers and chooses pause/spill/abort behavior under RSS pressure. | `crates/clinker-exec/src/pipeline/memory.rs`; `docs/user/src/ops/memory.md`; `docs/engine/src/memory-arbitration.md` | MemoryArbitrator, ConsumerHandle, BackpressureKnob | High |
| `MemoryConfig` | Pipeline YAML block for memory limit and backpressure policy. | `crates/clinker-plan/src/config/pipeline.rs`; `docs/user/src/pipelines/structure.md`; `docs/user/src/ops/memory.md` | BackpressureKnob, memory.limit | High |
| `BackpressureKnob` | User-facing selector for memory arbitration policy: `spill`, `pause`, or `both`. | `crates/clinker-plan/src/config/pipeline.rs`; `docs/ai/30_DESIGN_RULES.md` | pause, spill, LargestFirst | High |
| Spill | Runtime fallback that writes buffered records/operator state to disk under memory pressure. | `docs/user/src/ops/memory.md`; `docs/engine/src/storage-internals.md`; `crates/clinker-exec/src/pipeline/spill.rs` | SpillConfig, CompressMode, node buffer | High |
| `SpillConfig` | Workspace `[storage.spill]` TOML settings for spill directory, disk cap, compression, and related behavior. | `crates/clinker-plan/src/config/storage.rs`; `docs/user/src/ops/storage.md` | clinker.toml, CompressMode, disk_cap_bytes | High |
| `CompressMode` | Spill compression mode: `auto`, `off`, or `on`, resolved against projected spill batch size. | `crates/clinker-plan/src/config/storage.rs`; `docs/ai/60_PERFORMANCE_NOTES.md` | spill, LZ4, postcard | High |
| Node buffer | Runtime inter-stage materialization slot for stage outputs that cannot stream directly to a single consumer. | `crates/clinker-exec/src/executor/node_buffer.rs`; `docs/engine/src/execution-model.md` | materialized buffer, spill, MemoryConsumer | High |
| `ConsumerHandle` | Memory-arbitrator handle used by runtime consumers to report bytes and react to pause/spill signals. | `crates/clinker-exec/src/pipeline/memory.rs`; `docs/ai/40_COMMON_PATTERNS.md` | MemoryConsumer, MemoryArbitrator | High |
| `StageMetrics` | Per-stage timing/RSS/CPU/IO/spill/heap-delta metrics collection surface. | `crates/clinker-exec/src/executor/stage_metrics.rs`; `docs/ai/60_PERFORMANCE_NOTES.md`; `docs/user/src/ops/metrics.md` | ExecutionMetrics, MetricsConfig | High |
| Metrics spool | Per-run JSON metrics output directory later swept by `clinker metrics collect` into an archive. | `crates/clinker-plan/src/config/pipeline.rs`; `crates/clinker/src/main.rs`; `docs/user/src/ops/metrics.md` | MetricsConfig, StageMetrics | High |
| `clinker.toml` | Workspace-level TOML config file, currently modeled for storage spill/staging settings. | `crates/clinker-plan/src/config/storage.rs`; `examples/pipelines/clinker.toml` | StorageConfig, workspace | High |
| Staging | Optional copy of matched source files to local disk before execution, with content-addressed files, manifests, and locks. | `crates/clinker-plan/src/config/storage.rs`; `crates/clinker-channel/src/staging_copy.rs`; `docs/engine/src/storage-internals.md` | SourceStager, StagingPolicy, manifest | High |
| Staging manifest | Sidecar JSON file recording staged source identity, size, mtime, BLAKE3 hash, and stage time; commit marker for a trusted staged copy. | `docs/engine/src/storage-internals.md`; `crates/clinker-channel/src/staging_copy.rs` | `.staged`, `.manifest.json`, BLAKE3 | High |
| `SourceStager` | Channel crate type that performs staging-copy behavior consumed by runtime setup. | `crates/clinker-channel/src/lib.rs`; `crates/clinker-channel/src/staging_copy.rs`; `crates/clinker-channel/tests/staging_reuse_concurrent.rs` | StagingPolicy, staging manifest | High |
| Retraction protocol | Runtime protocol for relaxed correlation-key aggregates: retract failing records' contributions and refinalize affected groups instead of DLQ'ing whole aggregate groups. | `docs/engine/src/retraction-protocol.md`; `docs/engine/src/retraction-cost-reference.md`; `crates/clinker-exec/src/executor/commit/` | relaxed CK, `$ck.aggregate.<name>`, recompute | High |
| Strict correlation path | Aggregate path used when `group_by` covers all visible correlation-key fields; whole-group collateral behavior applies. | `docs/engine/src/retraction-protocol.md`; `docs/user/src/pipelines/correlation-keys.md`; `crates/clinker-exec/tests/strict_pipeline_zero_overhead.rs` | retraction path, group_by, CK | High |
| Relaxed correlation path | Aggregate path used when `group_by` omits a visible correlation-key field, activating retraction/recompute behavior. | `docs/engine/src/retraction-protocol.md`; `crates/clinker-exec/tests/post_aggregate_recompute_determinism.rs` | retraction protocol, E15Y | High |
| E15Y | Plan-time diagnostic code for retraction-mode aggregate incompatibility with `strategy: streaming`. | `docs/engine/src/retraction-protocol.md`; `docs/explain/E15Y.md` | streaming aggregate, retraction | High |
| Reversible accumulator | Aggregate state that can remove a row's contribution directly during retraction, such as `sum`, `count`, `collect`, and `any`. | `docs/engine/src/retraction-protocol.md`; `crates/clinker-record/src/accumulator/mod.rs` | Reversibility, retraction | High |
| BufferRequired accumulator | Aggregate state that must keep raw contributions and recompute affected groups on retraction, such as `min`, `max`, `avg`, and `weighted_avg`. | `docs/engine/src/retraction-protocol.md`; `crates/clinker-record/src/accumulator/mod.rs` | Reversibility, retraction | High |
| Driver input | Combine input selected as the probe/driver side; unmatched behavior and output cardinality are driver-relative. | `docs/user/src/nodes/combine.md`; `crates/clinker-plan/src/plan/combine.rs`; `crates/clinker-exec/src/executor/combine_dispatch.rs` | drive, build side, on_miss | High |
| Build side | Combine input(s) materialized or indexed before probe/driver matching. | `docs/user/src/nodes/combine.md`; `crates/clinker-exec/src/pipeline/combine.rs`; `docs/engine/src/execution-model.md` | driver, hash table, grace hash | High |
| IEJoin | Range-join algorithm used by combine for supported range-predicate shapes. | `crates/clinker-exec/src/pipeline/iejoin.rs`; `docs/ai/60_PERFORMANCE_NOTES.md`; `crates/clinker-exec/tests/combine_iejoin_prop.rs` | range conjunct, sort-merge, combine | High |
| Grace hash join | Disk-spilling partitioned hash join strategy for combine when build-side data does not fit memory or strategy hints request it. | `crates/clinker-exec/src/pipeline/grace_hash/`; `crates/clinker-plan/src/config/aggregate.rs`; `docs/ai/60_PERFORMANCE_NOTES.md` | CombineStrategyHint, spill, build side | High |
| Sort-merge join | Combine strategy for ordered/range-compatible inputs that sorts or merges instead of hash-probing. | `crates/clinker-exec/src/pipeline/sort_merge_join.rs`; `crates/clinker-exec/tests/fixtures/combine/sort_merge_price_range.yaml` | combine, range conjunct, sort | High |
| Build sketches | Approximate summaries such as Bloom, HLL, and Misra-Gries used for build-side statistics. | `crates/clinker-exec/src/sketch/`; `crates/clinker-exec/tests/explain_arbitration.rs`; `docs/ai/60_PERFORMANCE_NOTES.md` | Bloom, Hll, MisraGries | Medium |
| Watermark runtime | Executor tracking of per-source event-time progress used by time-window close decisions. | `crates/clinker-exec/src/executor/watermark.rs`; `docs/user/src/nodes/source.md`; `docs/user/src/nodes/aggregate.md` | WatermarkConfig, time_window | High |
| `WindowRuntimeRegistry` | Runtime registry for analytic/window arenas and indexes shared by transform/window evaluation. | `crates/clinker-exec/src/executor/window_runtime.rs`; `docs/ai/40_COMMON_PATTERNS.md` | Arena, SecondaryIndex, `$window.*` | High |
| `WriterRegistry` | Runtime registry that owns output writers and lazy writer construction. | `crates/clinker-exec/src/executor/registry.rs`; `docs/ai/40_COMMON_PATTERNS.md` | Output, FormatWriter | High |
| `ShutdownToken` | Runtime cancellation token polled by sources/operators to stop cleanly. | `crates/clinker-exec/src/pipeline/shutdown.rs`; `crates/clinker-exec/src/source/mod.rs` | RecordSource, cancellation | High |
| Per-source rollback cursor | Execution-report map tracking the highest cleanly exited row per source, rewound on correlated failures for resume/replay anchoring. | `docs/engine/src/correlation-lifecycle.md`; `crates/clinker-exec/tests/per_source_rollback.rs`; `crates/clinker-exec/src/executor/params.rs` | retraction, correlation key | High |

## Format Terms

| Term | Meaning | Where it appears | Related terms | Confidence |
|---|---|---|---|---|
| CSV | Delimited text input/output format supported by `clinker-format`. | `docs/user/src/formats/csv.md`; `crates/clinker-format/src/csv/`; `examples/pipelines/*.yaml` | CsvOutputOptions, CsvDialect | High |
| JSON | JSON array, NDJSON, and wrapper-object format support, including streaming body paths and envelope indexing. | `docs/user/src/formats/json.md`; `crates/clinker-format/src/json/`; `docs/user/src/pipelines/envelope-and-doc-context.md` | NDJSON, json_pointer, DocArenaIndex | High |
| NDJSON | Newline-delimited JSON record stream variant. | `docs/user/src/formats/json.md`; `benches/pipelines/format/json_ndjson_passthrough.yaml`; `crates/clinker-format/src/json/reader.rs` | JSON, streaming | High |
| XML | XML input/output format support with record paths and envelope section extraction. | `docs/user/src/formats/xml.md`; `crates/clinker-format/src/xml/`; `docs/user/src/pipelines/envelope-and-doc-context.md` | xml_path, envelope, document index | High |
| Fixed-width | Column-positioned legacy extract format whose `schema:` columns carry byte layout (`start`/`width`), justification, and truncation policies alongside the CXL type. | `docs/user/src/formats/fixed-width.md`; `crates/clinker-format/src/fixed_width/`; `crates/clinker-format/src/schema.rs` | Column, discriminator, LineSeparator | High |
| HL7 | HL7 v2 pipe-and-hat message format support with nested message/envelope behavior. | `docs/user/src/formats/hl7.md`; `crates/clinker-format/src/hl7/`; `crates/clinker-exec/tests/hl7_pipeline.rs` | FHS, BHS, MSH, envelope | High |
| X12 | ANSI ASC X12 EDI format support with ISA/GS/ST nesting and structural counts. | `docs/user/src/formats/x12.md`; `crates/clinker-format/src/x12/`; `crates/clinker-exec/tests/x12_pipeline.rs` | EDI, ISA, GS, ST, envelope | High |
| EDIFACT | UN/EDIFACT interchange format support. | `docs/user/src/formats/edifact.md`; `crates/clinker-format/src/edifact/`; `crates/clinker-exec/tests/edifact_pipeline.rs` | EDI, UNB, UNT, UNZ | High |
| SWIFT MT | SWIFT FIN message format support. | `docs/user/src/formats/swift.md`; `crates/clinker-format/src/swift/`; `crates/clinker-exec/tests/swift_pipeline.rs` | SwiftReader, SwiftWriter | High |
| EDI | Electronic data interchange family represented here by EDIFACT and X12 format support. | `docs/user/src/formats/edifact.md`; `docs/user/src/formats/x12.md`; `docs/user/src/nodes/envelope.md` | X12, EDIFACT, envelope | High |
| Multi-record file | CSV or fixed-width file containing different record types identified by a discriminator, often header/body/trailer. | `crates/clinker-format/src/multi_record.rs`; `docs/user/src/formats/csv.md`; `docs/user/src/pipelines/envelope-and-doc-context.md` | discriminator, record_type extract | High |
| `SourceSchema` / `Column` | The unified per-source schema. One `schema:` key carries a `SourceSchema` (a `Columns` list, a multi-record `discriminator`/`records` map, an EDI `generated` marker, or an external `.schema.yaml` file); each `Column` carries one CXL `type` that drives both byte parsing and typecheck, plus any physical-layout/formatting attributes. Replaces the retired split `format_schema:` + inline `schema:`. | `crates/clinker-format/src/schema.rs`; `crates/clinker-plan/src/schema/mod.rs`; `docs/user/src/nodes/source.md` | Column, generated, multi-record | High |
| Document envelope | Document-level sections surrounding body records, exposed through `$doc.*` and optionally reconstructed by outputs. | `docs/user/src/pipelines/envelope-and-doc-context.md`; `crates/clinker-format/src/envelope.rs`; `crates/clinker-record/src/document_context.rs` | `$doc.*`, Envelope node, DocumentContext | High |
| Document context | Per-record context carrying source file, document grain, envelope sections, and document metadata. | `crates/clinker-record/src/document_context.rs`; `crates/clinker-format/src/traits.rs`; `docs/user/src/pipelines/envelope-and-doc-context.md` | DocumentId, DocumentGrain, EnvelopeRecord | High |
| Document grain | Logical document identity used for framing/reconstruction; not always one-per-file. | `crates/clinker-record/src/document_context.rs`; `docs/user/src/nodes/envelope.md` | Envelope, header, concat, preserve | High |
| `EnvelopeRecord` | Record-like representation of an envelope section/header/footer. | `crates/clinker-record/src/document_context.rs`; `docs/user/src/nodes/envelope.md` | DocumentContext, OutputEnvelopeSpec | High |
| `EnvelopeConfig` | Source-side declaration of envelope sections and extraction rules. | `crates/clinker-format/src/envelope.rs`; `crates/clinker-plan/src/config/source.rs`; `docs/user/src/pipelines/envelope-and-doc-context.md` | `$doc.*`, EnvelopeSection, EnvelopeExtract | High |
| `EnvelopeEvent` | Format-layer event used to open/close nested envelope levels during ingest. | `crates/clinker-format/src/envelope.rs`; `crates/clinker-format/src/traits.rs`; `crates/clinker-exec/src/executor/ingest.rs` | Punctuation, DocumentContext | High |
| Document index | Bounded retained map of declared envelope sections for JSON/XML pre-scan and `$doc.*` lookup. | `crates/clinker-format/src/doc_index.rs`; `docs/user/src/pipelines/envelope-and-doc-context.md` | DocArenaIndex, max_index_bytes | High |
| `DocArenaIndex` | Format crate document-index type exported for retained envelope/document context lookup. | `crates/clinker-format/src/lib.rs`; `crates/clinker-format/src/doc_index.rs`; `docs/ai/40_COMMON_PATTERNS.md` | document index, `$doc.*` | High |
| `max_index_bytes` | JSON/XML cap on retained envelope-section index bytes. | `docs/user/src/pipelines/envelope-and-doc-context.md`; `crates/clinker-format/src/json/reader.rs`; `crates/clinker-format/src/xml/reader.rs` | document index, envelope | High |
| `ReopenableSource` | Abstraction for inputs that can be reopened or buffered enough for multi-pass readers without eagerly loading file-backed inputs. | `crates/clinker-format/src/source.rs`; `docs/ai/40_COMMON_PATTERNS.md`; `docs/ai/60_PERFORMANCE_NOTES.md` | SourceIdentity, multi-pass, FormatReader | High |
| `SourceIdentity` | Stable identity for source bytes used to verify reopened sources. | `crates/clinker-format/src/source.rs`; `docs/ai/40_COMMON_PATTERNS.md` | ReopenableSource, source file | High |
| BOM | Byte-order mark handling wrapper used by text format readers. | `crates/clinker-format/src/bom.rs`; `docs/ai/60_PERFORMANCE_NOTES.md` | CSV, text formats | High |
| `CountingWriter` | Writer wrapper that tracks bytes written for output splitting and metrics. | `crates/clinker-format/src/counting.rs`; `crates/clinker-format/src/traits.rs` | SharedByteCounter, bytes_written, split | High |
| `SplittingWriter` | Format-layer writer that rotates output files by split policy. | `crates/clinker-format/src/splitting/mod.rs`; `crates/clinker-plan/src/config/output.rs`; `docs/user/src/cookbook/file-splitting.md` | SplitPolicy, SplitConfig | High |
| `OutputEnvelopeSpec` | Format-local spec consumed by output envelope framing writers. | `crates/clinker-format/src/envelope_writer.rs`; `crates/clinker-plan/src/config/output.rs` | EnvelopeFramer, reconstruct_envelope | High |
| `EnvelopeFramer` | Writer-side helper for reconstructing envelope header/footer around output documents. | `crates/clinker-format/src/envelope_writer.rs`; `crates/clinker-format/src/lib.rs` | OutputEnvelopeSpec, begin_document | High |

## Review Notes

- The glossary intentionally avoids generic Rust terms unless Clinker gives them a project-specific role.
- `docs/user/src/getting-started/concepts.md` still says "eight node types"; current `PipelineNode` code has eleven variants. See `docs/ai/80_OPEN_QUESTIONS.md`.
- CXL is defined by behavior and crate boundaries here; the acronym expansion itself remains unclear and is tracked in `docs/ai/80_OPEN_QUESTIONS.md`.
