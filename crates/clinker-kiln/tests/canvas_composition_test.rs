//! Playwright MCP integration tests for composition drill-in on the Kiln canvas.
//!
//! These tests run against the web build of clinker-kiln (`dx serve --platform web`).
//! Desktop (wry) targets cannot be driven by Playwright.

#[test]
fn scaffold() {
    // Placeholder — replaced by Playwright MCP tests in 16c.5.2+
}
